# TypeScript / React Guidelines

- Use `strict` TypeScript and prefer `zod` for runtime validation.
- Prefer functional components and hooks; avoid anti-patterns like excessive prop drilling.
- Run `npm test` and `eslint` before PRs.