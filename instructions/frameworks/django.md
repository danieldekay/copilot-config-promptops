# Django Notes

- Use Django ORM with parameterized queries; avoid raw SQL unless necessary.
- Keep views thin; implement business logic in services.
- Use Django migrations for schema changes and write migration tests.